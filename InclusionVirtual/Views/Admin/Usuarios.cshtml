@{
    Layout = "~/Views/Shared/Ajustes_Layout.cshtml";
    ViewData["Title"] = "Usuarios";
    var usuarios = (List<Usuario>)Model;
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Usuarios - @ViewBag.Nombre</h1>
    <div class="card mb-4">
        <div class="card-header">
            <i class="fas fa-table me-1"></i>
            Tabla de @ViewBag.Nombre
        </div>
        <div class="card-body">
            <table id="datatablesSimple">
                <thead>
                    <tr>
                        <th></th>
                        <th>Nombres y Apellidos</th>
                        <th>Celular</th>
                        <th>Correo</th>
                        <th>Usuario</th>
                        <th></th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th></th>
                        <th>Nombres y Apellidos</th>
                        <th>Celular</th>
                        <th>Correo</th>
                        <th>Usuario</th>
                        <th></th>
                    </tr>
                </tfoot>
                <tbody>
                    @foreach (var item in usuarios)
                    {
                        <tr>
                            <td><img width="25" src="@item.Imagen" alt=""></td>
                            <td>@item.Nombres @item.Apellidos</td>
                            <td>@item.Celular</td>
                            <td>@item.Correo</td>
                            <td>@item.Username</td>
                            <td class="float-right">
                                <a class="btn btn-success btn-sm" href="/admin/EditarUsuario?idUser=@item.Id"><i class="fas fa-edit"></i></a>
                                <a class="btn btn-danger btn-sm" onclick="Eliminar(@item.Id ,'usuario')"><i class="fas fa-trash-alt"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts{
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/simple-datatables@latest" crossorigin="anonymous"></script>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <!-- JS -->
    <script src="~/js/datatables-simple-demo.js" asp-append-version="true"></script>
}